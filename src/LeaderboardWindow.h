#pragma once
#include <string>

void showLeaderboard(unsigned cols,unsigned rows,
                     const std::string& playerName,
                     int elapsedSeconds,
                     bool fromVictory);
